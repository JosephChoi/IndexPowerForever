# 은퇴 후 인출전략 시뮬레이터 — 개발 계획서

> 📖 **사용자용 매뉴얼**: [docs/인출전략시뮬레이터_매뉴얼.md](docs/인출전략시뮬레이터_매뉴얼.md)
> 이 문서는 개발 과정과 설계 근거를 기록합니다. 사용법·원리는 매뉴얼을 참조하세요.

> 작성일: 2026-07-26
> 위치: 퇴직연금 시뮬레이터(`/retirement`) 페이지 **하단 섹션**으로 추가
> 대상 지수: **S&P 500(SPY) / NASDAQ 100(QQQ) 2종만**

---

## 1. 개요

### 1-1. 무엇을 보여주는가

기존 퇴직연금 시뮬레이터가 "**모으는 단계**"(적립)를 다룬다면,
이 시뮬레이터는 "**쓰는 단계**"(인출)를 다룹니다.

> **핵심 질문**: "10억을 QQQ에 넣고 매달 300만원씩 빼서 쓰면, 그 돈은 몇 년 갈까?"

기존 시뮬레이터와의 결정적 차이:

| 항목 | 퇴직연금 시뮬레이터(기존) | 인출전략 시뮬레이터(신규) |
|---|---|---|
| 수익률 | 연 10%/13% **고정 가정** | **2002년~현재 실제 일별 가격** |
| 방향 | 적립(+) | 인출(−) |
| 핵심 리스크 | 없음 | **수익률 배열 위험(Sequence of Returns Risk)** |

**수익률 배열 위험**이 이 시뮬레이터의 존재 이유입니다. 평균 수익률이 같아도 **초반에 폭락이 오면** 하락장에서 자산을 팔아 인출하게 되고, 회복기에 태울 원금이 남지 않아 자산이 조기 고갈됩니다. 리저브(현금 버퍼) 전략은 정확히 이 문제를 방어하기 위한 장치입니다.

### 1-2. 왜 2002년부터인가

QQQ 상장이 1999-03-10이라 닷컴 버블 붕괴 직전 구간이 포함됩니다. 2002년 시작은 "**닷컴 폭락 직후 바닥에서 시작**"하는 유리한 시나리오이므로, 시작연도를 사용자가 조정할 수 있게 하여 **1999년 시작(최악의 배열)**과 비교할 수 있도록 합니다. 이 대비가 콘텐츠적으로 가장 강력합니다.

---

## 2. 입력 사양

> **입력 방식: 슬라이더 → 숫자 직접 입력으로 변경** (Joseph 요청)
> 슬라이더 드래그는 금융 도구로서 전문성이 떨어져 보입니다. 정확한 금액을 타이핑하는 편이
> 은퇴 설계처럼 구체적 숫자를 다루는 맥락에 맞습니다. 대신 자주 쓰는 값은 칩으로 제공하고,
> 입력값 아래에 사람이 읽는 단위(`4억원`, `연 1,800만원`)를 되짚어 표시합니다.

| # | 항목 | UI | 값/범위 | 기본값 |
|---|---|---|---|---|
| 0 | 지수 선택 | 토글 버튼 2개 | `SPY`(S&P 500) / `QQQ`(NASDAQ 100) | SPY |
| 1 | 초기 자산 | **숫자 입력 + 빠른 선택 칩** | 1,000 ~ 1,000,000만원 / 칩: 3·5·10·20억 | 40,000만원(4억) |
| 2 | 인출 주기 | 토글 | `monthly` / `annual` | monthly |
| 3 | 인출액 | **숫자 입력** | 1 ~ 초기자산의 50%(연환산) | 월 150만원 |
| 3-b | 물가연동 | 숫자 입력 | 연 0~5% 인출액 자동 증액 | 0% |
| 4 | 시작 연도 | **숫자 입력** | 티커별 상장연도 ~ (현재-1) — SPY 1993 / QQQ 1999 | 2002 |
| 4-b | 운용 기간 | (내부 고정) | 현재까지 | 현재까지 |
| 5 | 리저브 사용 | 스위치 | on/off | **on** |
| 5-a | 리저브 크기(목표) | **숫자 입력** | 인출액 기준 **1~5년치** | **3년치** |
| 5-b | 리저브 하한선 | **숫자 입력** | **0.5년 ~ (크기−0.5년)** (리필 트리거) | **1년치** |
| 5-c | 리저브 이자율 | **숫자 입력** | **연 0~6%**, step 0.5% (파킹통장·MMF) | **연 3%** |

**직접 입력의 방어 처리**
- 보정은 `@blur` 시점에만 — `@input`에서 clamp하면 "40000" 타이핑 중 "4"가 최솟값으로 튀어 입력이 막힘
- `simulate()` 진입 시 불완전한 값이면 요청 자체를 건너뜀 (에러 메시지 깜빡임 방지)
- 인출액 상한은 초기자산의 50%(연환산)에 맞춰 자동 계산 — 백엔드 검증과 일치
- 하한선 최댓값은 `리저브 크기 − 0.5`로 연동

> ~~목표 수익률(%)~~ 항목은 **폐기**했습니다. 사유는 2-1 참조.

**입력 검증(백엔드)**
- ticker: `SPY` 또는 `QQQ`만 허용 (그 외 ValidationError)
- initialAmount: 100 ~ 1,000,000 (만원)
- withdrawal > 0, 초기자산 대비 연 인출률 50% 초과 시 ValidationError
- startYear: 1999 ~ 현재-1
- reserveFloorYears < reserveYears (하한선이 목표보다 크면 ValidationError)
- 리저브 총액(인출액 × 목표연수) ≥ 초기자산이면 ValidationError

### 2-1. 리저브 전략 규칙 — **금액 기준 + 조건부 리필 (확정)**

**왜 수익률 기준을 버렸는가**

당초 "목표수익률 15% 달성 시 리필" 안을 제시했으나 폐기했습니다. "3년에 30%를 목표로 하고, 1년 만에 달성하면 남은 기간엔 더 보수적인 자산으로 갈아탄다"는 전략은 **투자대상을 바꿀 수 있을 때** 성립합니다. 이 시뮬레이터는 SPY/QQQ로 대상이 고정되어 있어 목표 달성 후 취할 행동이 없고, 따라서 수익률 목표가 겉돕니다.

대신 **리저브 잔액 자체를 지표로** 삼습니다. "생활비가 몇 년치 남았는가"는 사용자가 직관적으로 이해하는 단위이고, 차트에서도 그대로 읽힙니다.

**리필 규칙 (안 ③ — 결합형, 확정)**

```
[초기 세팅]
  reserveTarget = 연인출액 × 목표연수        예) 3,000만 × 3년 = 9,000만
  reserveFloor  = 연인출액 × 하한연수        예) 3,000만 × 1년 = 3,000만
  reserve       = reserveTarget              (현금 파킹, 연 3% 이자)
  invested      = 초기자산 − reserve         (지수 매수)
  lastRefillBasis = invested                 (② 판정용 기준가)

[매 인출 시점]
  reserve ≥ 인출액  →  reserve에서 인출 (지수 자산 무손상) ✅ 핵심 방어
  reserve <  인출액  →  부족분만 지수 매도로 인출

[매 인출 시점 — 리필 판정]
  ⚠️ 리필은 reserve < reserveFloor 일 때만 검토한다 (아래 "리필 트리거" 참조)

  if reserve >= reserveFloor: 리필 없음

  ② 기회 리필 — invested > lastRefillBasis   ← 직전 리필 시점보다 자산이 위
       → 이익 구간에서 파는 것. refillType = 'opportunistic'
  ① 강제 리필 — 그 외 (손실 구간인데 리저브가 바닥)
       → 어쩔 수 없이 파는 것. refillType = 'forced'  (위험 신호, 적색 표시)

  실행 시 reserveTarget까지 채움 (invested 부족하면 남은 전액만 = 부분 리필)
  lastRefillBasis = 매도 "직전" invested 로 갱신
```

**⚠️ 리필 트리거는 하한선 — 검증에서 확인된 필수 조건**

당초 "부족하면 언제든 채움(needed > 0)"으로 구현했다가 **리저브 전략이 오히려 손해**로 나왔습니다. 매 인출 시점마다 리저브를 가득 채우다 보니 상승장 내내 지수 자산을 팔아 연 3% 현금으로 옮기게 되어, 연 10% 자산을 3%짜리로 바꾸는 꼴이었습니다. (QQQ 1999 케이스에서 리필 95회 발생 → 리저브 ON이 OFF보다 먼저 고갈)

**리저브는 "비면 채우는" 버퍼이지 "항상 가득 유지하는" 통이 아닙니다.** 트리거를 하한선으로 바꾼 뒤 리필 12회로 줄고 모든 케이스에서 ON이 우위로 전환됐습니다.

또한 `lastRefillBasis`를 **매도 직후** 값으로 갱신하면 기준선이 계속 내려가 조금만 올라도 이익 구간으로 오판합니다. 반드시 **매도 직전** 값으로 잡아야 합니다.

**설계 의도**: 오를 때는 미리 현금을 쟁여두고(②), 장기 하락장에서 리저브가 위험 수위까지 마르면 손해를 감수하고라도 채운다(①). ②만 쓰면 2000~2002년 같은 3년 연속 하락장에서 리저브가 고갈돼 결국 바닥에서 팔게 되고, ①만 쓰면 상승장에서 채워둘 기회를 놓칩니다. 결합해야 방어와 생존이 모두 확보됩니다.

**결과 표시**: `forced` 리필이 발생한 해는 **"손실 구간에서 강제 매도"**로 표시합니다. 이 횟수 자체가 전략의 취약점을 드러내는 지표라 요약 카드에도 노출합니다.

---

## 3. 확정 사항 (전부 결정 완료 ✅)

| # | 쟁점 | 확정안 |
|---|---|---|
| A | 리저브 리필 기준 | **금액 기준 + 결합형 리필** (2-1 참조) |
| B | 리저브 파킹 수익률 | **사용자 입력** — 슬라이더 연 0~6%, 기본 3% |
| C | 배당 반영 | **미반영** — 순수 주가(Price) 기준 |
| D | 자산 고갈 시 처리 | **0으로 표시 후 종료** + 고갈까지 걸린 기간 명시 |
| E | 세금/수수료 | **미포함** — 면책 문구로 처리 |

### 3-1. 각 확정안의 구현 영향

**C. 배당 미반영 — 데이터 변경 불필요 ✅**
`YahooService`가 이미 `indicators.quote[0].close`(순수 주가, 배당 미반영)를 저장하고 있어 **현재 `price_cache` 데이터를 그대로 사용**하면 됩니다. Adjusted Close였다면 재적재가 필요했지만 그럴 일이 없습니다.

> ⚠️ 다만 배당 미반영은 결과를 **보수적(비관적) 방향으로** 치우치게 합니다. SPY 배당수익률이 약 1.3%, QQQ가 약 0.6%라 30년 시뮬레이션에서는 무시할 수 없는 차이입니다. 실제로는 배당금도 인출 재원이 되므로, **"배당은 반영하지 않아 실제보다 보수적인 결과"**라는 문구를 면책 안내에 명시합니다. 사용자에게 유리한 방향의 오차가 아니라 불리한 방향이므로 안전한 선택입니다.

**D. 고갈 처리 — 표시 사양**
```
자산 = 0 도달 시 시뮬레이션 중단
  → 요약 카드: "고갈" 적색 표시
  → "N년 M개월 만에 고갈 (YYYY년 M월)" 명시
  → 차트: 고갈 시점에 수직 마커, 이후 구간은 그리지 않음
  → 연도별 표: 고갈 연도까지만 출력, 마지막 행 적색 강조
```

**E. 세금 미포함 — 면책 문구**
> "본 시뮬레이션은 과거 실제 지수 데이터를 기반으로 하며, 배당·세금·거래비용은 반영하지 않았습니다. 과거 성과가 미래 수익을 보장하지 않습니다."

---

## 4. 결과 사양

### 4-1. 통합 차트 (요청하신 0·1·2를 하나로)

**이중 Y축 라인 차트** — 스케일이 다른 3개 데이터를 한 화면에 겹칩니다.

| 데이터 | 축 | 타입 | 색 |
|---|---|---|---|
| ① 지수 자체 흐름 (시작=100 정규화) | 우측 Y (지수) | 라인, 얇음, 점선 | `#a3aab8` 회색 |
| ② 내 자산 평가액 (리저브 포함 총액) | 좌측 Y (원화) | 라인, 두꺼움 2.5px | SPY `#16a34a` / QQQ `#dc2626` |
| ②-b 리저브 잔액 (리저브 ON일 때만) | 좌측 Y | 영역, 반투명 | `#26b4a8` 청록 |
| ③ 인출 누계 | 좌측 Y | 라인 2px 대시 | `#d4af37` 골드 |

- **리저브 하한선을 수평 점선으로 표시** — 청록 영역이 이 선 아래로 내려가는 순간이 강제 리필 시점
- **리필 발생 지점에 포인트 마커** — 기회 리필 청록 원 / 강제 리필 적색 삼각형
- 자산 고갈 시점에 **수직 마커 + "N년 M개월 만에 고갈" 라벨**
- 리저브 ON/OFF 비교 모드: 리저브 사용 시 **OFF 시나리오를 반투명 회색 라인으로 동시 표시** → 전략 효과가 한눈에 보임 (강력 추천)
- `.chart-canvas-box`로 감싸기 (높이 400px, 모바일 280px)

### 4-2. 요약 카드 (차트 위)

```
[최종 자산]  [총 인출액]  [자산 유지 기간]  [MDD]  [강제 리필 횟수]
```
- 자산 고갈 시 최종 자산 카드는 적색 **"고갈"** + 하단에 `17년 6개월 만에 고갈 (2019년 7월)` 표시
- 미고갈 시 유지 기간 카드는 `24년 유지 중` 으로 표시
- **강제 리필 횟수**는 "손실 구간에서 어쩔 수 없이 판 횟수" — 0회면 전략이 완벽히 작동한 것

### 4-3. 연도별 표

| 연도 | 연초 자산 | 지수 수익률 | 인출액 | 리저브 잔액 | 남은 연수 | 리필 | 연말 자산 | 인출 누계 |
|---|---|---|---|---|---|---|---|---|
| 2002 | 4.0억 | −22.1% | 3,000만 | 6,000만 | 2.0년 | − | 3.0억 | 3,000만 |
| 2003 | 3.0억 | +28.7% | 3,000만 | 9,000만 | 3.0년 | 🔵 기회 | 3.3억 | 6,000만 |

- `.table-standard` 사용, `<div class="table-responsive">` 래퍼
- 지수 수익률 셀: 양수 `--color-outperform` / 음수 `--color-underperform`
- **남은 연수** = 리저브 ÷ 연인출액 — 하한선 미만이면 적색
- 리필 컬럼: `🔵 기회` / `🔴 강제` / `−`. 강제 리필 행은 좌측 적색 보더로 강조
- 모바일(<576px): 컬럼 9개 → 카드 레이아웃 전환

---

## 5. 기술 설계

### 5-1. 백엔드

**신규 파일 2개**

```
backend/src/routes/withdrawal.js          — 입력 검증 + 서비스 호출
backend/src/services/WithdrawalService.js — 인출 시뮬레이션 로직
```

**엔드포인트**

```
GET /api/withdrawal
  ?ticker=SPY|QQQ
  &initial=100000            (만원)
  &frequency=monthly|annual
  &withdrawal=300            (만원)
  &inflation=0               (%, 인출액 연증가율)
  &startYear=2002
  &years=0                   (0 = 현재까지)
  &reserve=0|1
  &reserveYears=3            (목표 리저브 = 연인출액 × 3)
  &reserveFloorYears=1       (하한선 = 연인출액 × 1, 강제 리필 트리거)
  &reserveRate=3             (리저브 파킹 이자율 %, 0~6)
```

**응답**

```json
{
  "data": {
    "ticker": "QQQ",
    "summary": {
      "finalAmount": 245000, "totalWithdrawn": 86400,
      "depletedAt": null,             // 고갈 시 "2019-07" 형태
      "survivedMonths": 288,          // 고갈까지 걸린 개월수
      "depletedLabel": null,          // 고갈 시 "17년 6개월 만에 고갈"
      "mdd": -52.7, "cagr": 6.8,
      "refillCount": 9, "forcedRefillCount": 2,
      "reserveTarget": 9000, "reserveFloor": 3000,
      "baselineFinalAmount": 210000   // 리저브 OFF 비교값
    },
    "timeline": [ { "date": "2002-01-31", "index": 100,
                    "asset": 100000, "reserve": 3600,
                    "withdrawnCum": 300, "refill": null } ],
    "yearly": [ { "year": 2002, "startAsset": 40000, "indexReturn": -22.1,
                  "withdrawn": 3000, "reserve": 6000, "reserveYearsLeft": 2.0,
                  "refill": null, "refillAmount": 0,
                  "endAsset": 30000, "withdrawnCum": 3000 } ]
  }
}
```

**서비스 로직 흐름**

```
WithdrawalService.simulate(params)
  → PriceService.get(ticker, 'max')            // 기존 서비스 재사용
  → 인출 시점(월말/연말) 인덱싱
  → 시점별 루프:
      자산 × 구간수익률 → 인출 처리(리저브 우선) → 리필 판정(강제 → 기회) → 기록
  → 리저브 ON이면 OFF 시나리오 동시 계산(비교선)
  → summary/timeline/yearly 조립
```

**캐시**: KV `withdrawal:{전체파라미터해시}` TTL 6h (입력 조합이 많아 히트율은 낮지만 슬라이더 재조정 시 유효)

**⚠️ 성능 주의**: 슬라이더 `@input`마다 API 호출하면 요청 폭주 → **프론트에서 400ms 디바운스 필수**.

### 5-2. 프론트엔드

**별도 페이지 `/withdrawal`로 확정** (Joseph 결정)

```
frontend/views/withdrawal.html    ← 신규
frontend/logic/withdrawal.js      ← 신규
frontend/logic/app.js             ← 라우트 추가
frontend/components/navbar.html   ← 시뮬레이터 드롭다운에 항목 추가
frontend/css/style.css            ← 인출전략 전용 클래스 추가
```

- `this._chart`를 Vue 반응성 밖(`created`)에 보관, `beforeUnmount`에서 destroy
- `this.$api.get('/api/withdrawal?...')` 사용 (직접 `fetch` 금지)
- Chart.js 색은 hex 직박 (CSS 변수 미해석)
- 슬라이더 조작은 400ms 디바운스로 요청 폭주 방지
- QQQ 선택 시 시작 연도 하한이 1999로 자동 보정 (상장일)
- 인출 주기 전환 시 연 인출액이 유지되도록 자동 환산

---

## 6. 작업 단계

| Phase | 작업 | 산출물 | 예상 |
|---|---|---|---|
| **0** | QQQ 데이터 커버리지 확인 (1999~현재 D1 적재 여부) — *3장 확정 완료* | 확인 | 즉시 |
| **1** | `WithdrawalService.js` 작성 — 시뮬레이션 엔진 + 결합형 리필 | 서비스 1개 | 핵심 |
| **2** | `withdrawal.js` 라우트 + `index.js` 마운트 | 라우트 1개 | 소 |
| **3** | ✅ 검증 완료 — 독립 수기계산 대조 + 버그 3건 수정 (아래 6-1) | 검증 로그 | 완료 |
| **4** | ✅ `withdrawal.html` 별도 페이지 (입력 패널 + 요약 카드 + 비교 배너) | 뷰 | 완료 |
| **5** | ✅ 통합 차트 + 연도별 표(PC/모바일) + 400ms 디바운스 + CSS | 로직 | 완료 |
| **6** | 반응형/디자인 가이드 준수 점검 + 면책 문구 | — | 소 |
| **7** | 커밋 & 푸시 (Joseph 승인 후) | 배포 | — |

---

### 6-1. 검증 결과 (Phase 1~3 완료)

**데이터**: SPY 8,427건(1993-01-29~), QQQ 6,885건(1999-03-10~) — 1999년 시작 시나리오까지 커버 확인

**검증 방법**: 엔진과 무관한 독립 수기 계산으로 리저브 OFF 시나리오를 대조 → 소수점까지 일치 확인

**수정한 버그 3건**

| # | 증상 | 원인 | 수정 |
|---|---|---|---|
| 1 | MDD가 −95~−99%로 무의미 | 인출로 줄어든 몫까지 낙폭에 포함 | 누적 인출액을 되더한 값으로 낙폭 계산 → SPY −23.63%, QQQ −66.25%(닷컴 실제치)로 정상화 |
| 2 | 상승장(+8.6%)에 `forced` 리필 표기 | 하한선 판정이 이익 판정보다 먼저 | 판정 순서 반전 — 이익 구간이면 하한선 미만이어도 `opportunistic` |
| 3 | **리저브 ON이 OFF보다 손해** | 매 시점 가득 채워 상승장에 지수→현금 이동 반복(리필 95회) | 리필 트리거를 하한선 미만으로 제한 |
| 4 | 상승장(+8.6%, +12.8%, +24.3%)에 `강제` 리필 표기 | 자산 **절대액**으로 이익/손실을 판정 — 인출·리필로 원금이 줄어 기준이 흔들림 | 판정 기준을 **매도 시점의 지수 가격**으로 변경. "직전 리필 때보다 비싸게 파는가"가 정확한 기준 |

**최종 결과 — 리저브 전략이 모든 케이스에서 우위**

| 케이스 | 리저브 OFF | 리저브 ON | 효과 |
|---|---|---|---|
| SPY 2002, 연 1,600만 (4%룰) | 9.25억 | **9.49억** | +0.24억 |
| SPY 2002, 연 3,000만 (7.5%) | 16년 고갈 | **18년 고갈** | +2년 |
| QQQ 1999, 연 3,000만 (최악 배열) | 11년 고갈 | **13년 고갈** | +2년 |
| QQQ 1999, 월 160만 | 총인출 3.18억 | **총인출 5.26억** | +2.08억 |

> **주의**: 4억에 연 3,000만은 인출률 7.5%로, 통상 안전 인출률(4%)의 두 배에 가깝습니다. 고갈은 정상적인 결과이며 **수익률 배열 위험**을 그대로 보여주는 사례입니다. 기본값을 4%룰 근처로 잡을지 검토가 필요합니다.

**정합성**: 음수 자산 0건 / 인출누계 단조증가 / 리저브 목표 초과 0건

---

## 7. 리스크

| 리스크 | 대응 |
|---|---|
| QQQ 1999~2001 데이터가 D1에 없을 수 있음 | Phase 0에서 확인, 없으면 Yahoo max로 1회 적재 |
| 배당 미반영으로 결과가 보수적으로 치우침 | **의도된 선택** — 사용자에게 불리한 방향의 오차라 안전. 면책 문구에 명시 (3-1 참조) |
| 슬라이더 조작 시 API 폭주 | 400ms 디바운스 + KV 캐시 |
| 이중 Y축 차트 가독성 저하 | 지수선은 회색 점선으로 후경 처리, 툴팁 단위 명시 |
| 통계 오해 (미래 보장으로 읽힘) | "과거 실제 데이터 기반이며 미래를 보장하지 않습니다" 면책 문구 고정 노출 |

---

## 8. 다음 액션

**확정 완료**
- 리필 규칙 — 금액 기준 + 결합형(강제 + 기회). 목표 수익률 입력 폐기
- 리저브 — 목표 3년치 / 하한선 1년치 / 이자율 사용자 입력(연 0~6%, 기본 3%)
- 배당 미반영, 세금 미포함, 고갈 시 0 표시 + 기간 명시

**남은 확인 2가지** (답 없으면 괄호 안으로 진행)

1. 배치 위치 — `/retirement` 하단 섹션(권장) vs 별도 페이지 `/withdrawal`
2. 리저브 OFF 비교선 동시 표시 — 포함(권장) vs 제외

이 2가지는 구현 후에도 변경 비용이 크지 않으므로, **지금 바로 Phase 0~3(백엔드)부터 착수 가능**합니다.
